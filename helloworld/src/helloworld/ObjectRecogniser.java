package helloworld;

public class ObjectRecogniser {

	public static void main(String[] args) {
		
		int min_row = 0;
		int min_col = 0;
		
		double min_mse = Double.MAX_VALUE;
		
		int patch_width = object.getWidth();
		int patch_height = object.getHeight();
		
		for(int row = 0; row < background.getHeight(); row++) {
			for(int col = 0; col < background.get.Width(); col++) {
				
				if (((row + patch_height) > background.getHeight()) || ((col + patch_width) > background.getWidth())) {
					
					
				}
			}
		}
		
		
		
		
	}
	
}
